# Zsh Configuration Guide

### Linking the dotfile

```bash
ln -sf ./zsh-config/.zshrc ~/.zshrc
ln -sf ./zsh-config/.zshenv ~/.zshenv
ln -sf ./zsh-config/.zimrc ~/.zimrc
ln -sf ./zsh-config/.p10k.zsh ~/.p10k.zsh
```
